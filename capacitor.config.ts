import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'DioPokedexProject',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
